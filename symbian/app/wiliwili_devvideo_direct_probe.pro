# Convenience entry point. The recommended Qt Creator setup still opens
# wiliwili_symbian.pro and adds CONFIG+=devvideodirectprobe1 so deployment
# keeps the verified production launch identity.
CONFIG += devvideodirectprobe1
include(wiliwili_symbian.pro)
